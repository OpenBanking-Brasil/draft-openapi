type: object
description: Objeto a ser retornado caso o consentimento seja rejeitado.
required:
  - "rejectedBy"
  - "reason"
properties:
  rejectedBy:
    $ref: ./EnumRejectedBy.yaml
  reason:
    $ref: ./RejectedReason.yaml
