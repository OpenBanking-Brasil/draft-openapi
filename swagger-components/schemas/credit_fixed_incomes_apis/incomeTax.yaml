type: object
description: valor do imposto considerando a alíquota vigente na data de referência
required:
  - amount
  - currency
properties:
  amount:
    $ref: ./amount.yaml
  currency:
    $ref: ./currency.yaml