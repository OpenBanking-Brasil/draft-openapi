type: object
description: valor bruto unitário atualizado na data de referência;
required:
  - amount
  - currency
  - quantity
properties:
  amount:
    $ref: ./updatedUnitPrice-amount.yaml
  currency:
    $ref: ./currency.yaml
  quantity:
    $ref: ./quantity.yaml