type: object
required:
  - data
  - links
  - meta
properties:
  data:
    type: array
    minItems: 0
    description: Conjunto de informações referentes à identificação da operação de crédito de empréstimo
    items:
      $ref: ./LoansWarranties.yaml
  links:
    $ref: ../Links.yaml
  meta:
    $ref: ../MetaOnlyRequestDateTime.yaml
additionalProperties: true
