type: object
required:
  - data
  - links
  - meta
properties:
  data:
    type: array
    minItems: 0
    items:
      $ref: ./ResourceData.yaml
    description: 'Lista de recursos e seus respectivos status.'
  links:
    $ref: ../Links.yaml
  meta:
    $ref: ../MetaOnlyRequestDateTime.yaml
additionalProperties: false